export interface Appointment {
          id: number
          docId?: string,
          hairdressigId: number;
          makeupId: number;
          nailsId: number;
          crearAt:string;
          dateTime:string;
}
