export interface Appointment {
          id: number
          docId?: string,
          hairdressigId?: string;
          makeupId?: string;
          nailsId?: string;
          createdAt: string;
          dateTime: string;
}
